framework:
    default_locale: '%default_locale%'
    enabled_locales: ['en', 'fr', 'ja']
    set_locale_from_accept_language: true
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
