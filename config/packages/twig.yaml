twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes:
        - '@LiveComponent/form_theme.html.twig'
        - 'App/_partials/_form_theme.html.twig'
    globals:
        map_tiles: '%app.map_tile_url%'
        map_attribution: '%app.map_attribution%'

when@test:
    twig:
        strict_variables: true
